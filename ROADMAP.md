# Roadmap

Milestone definitions are centralized in `MILESTONES.md`, and milestone status records are centralized in `MILESTONE_STATUS.md`. The roadmap is now a pointer to those authoritative documents to avoid fragmented milestone definitions.
